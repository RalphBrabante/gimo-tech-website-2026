{
  "name": "gimo-tech-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "server.js",
  "engines": { "node": "20.x || 22.x", "npm": ">=10" },
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -n API,CLIENT -c blue,magenta \"npm run dev --prefix server\" \"npm run watch --prefix client\"",
    "build": "npm ci --prefix server --include=dev && npm run build --prefix server && npm ci --prefix client --include=dev && npm run build --prefix client",
    "start": "node server.js",
    "verify": "npm run build"
  },
  "devDependencies": { "concurrently": "^9.0.1" }
}
